This is a simple `ERC20` token contract used as the underlying asset in the `ERC4626` vault demo. Deploy this contract first, then use its address when deploying the `ERC4626` vault in the next step.
